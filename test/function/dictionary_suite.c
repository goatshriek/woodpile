#include <stdlib.h>
#include <stdio.h>
#include <string.h>

#include <woodpile/dictionary.h>

#include "test/helper.h"

main
( void )
{
  unsigned failure_count = 0;
  const char * result;

  if( failure_count > 0 )
    return EXIT_FAILURE;
  else
    return EXIT_SUCCESS;
}
